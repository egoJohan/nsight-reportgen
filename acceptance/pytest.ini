; The acceptance suite is a separate thing from tests/ and is never collected by
; a bare `pytest` at the repo root (whose testpaths is ["tests"]). It carries its
; own config because without these two settings nothing here can import the
; product or run an async route.
[pytest]
pythonpath = ../src
asyncio_mode = auto
testpaths = tests

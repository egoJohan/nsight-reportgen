work/
.venv/
__pycache__/
*.pyc
.pytest_cache/

.superpowers/

# Local-only client SAV copies for demo-group e2e (IPR — never commit)
# Ignore the binaries but keep the README that explains how to populate them.
tests/rb/e2e/data/sav/
